"""Venue calendar adapter: which days each exchange traded, and between when.

Backed by the `exchange_calendars` package, whose holiday rules are the fact
about the outside world that `CalendarPort` needs and `core/` may not hold.
"""
